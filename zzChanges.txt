Stop reading commit messages Amanda you're cheating

========== V3.1 ========================
________> Fix
- Fix cancel button every fucking where
- Fix sound popping under wrong conditions

________> New feature
- profile can now be edited
- Forget my password feature
- Carousel image preview
- Fuzzy finder with basic route options
- Reply text preview
- Now support link in texts
- Now support newlines in texts
- Secret random background
- Emoji Picker
- App new name !
- options picker
- poll maker
- poll display w/ votes

________> Changed Behavior
- Better app gestion of pages (invisible feature)
- Text color get fucked when too much scrolling ???
- Limit amount of images

________> QOL
- better placeholder default profile pic
- Better layout for multiple images in a text

----------------------------------------------------------------

========== V3.2 ========================
________> Fix
- Can now insert newline inside text and not only at the end

========== To do ========================
- On paste scale text area to number of newlines
- Still fucking scrollbar on 24" display
- Finish Answer UI 
- Change password
- websocket
- on reply icon redirect to replied text in convo
- texts creating dick detector

const props = defineProps({
  previousText: {},
  message: {},
  nextText: {},
  user: {},
  responseMessageTransfer: Function,
  deleteMessage: Function,
  editMessage: Function,
  isCurrentUser: Boolean
})

const dickShape = isDickShape()

function isDickShape() {
  if (props.previousText && props.nextText) {
    const prevLength = props.previousText.value.length
    const currLength = props.message.value.length
    const nextLength = props.nextText.value.length

    if (prevLength < currLength && currLength < nextLength) {
      return true
    }
  }
  return false
}